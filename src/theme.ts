import { createTheme } from "@mui/material/styles";

// TODO: Add Dark mode baby
export const theme = createTheme({
  palette: {
    text: {
      primary: "#040316",
    },
    primary: {
      main: "#FFFFFF",
    },
    secondary: {
      main: "#694FD0FF",
    },
    background: {
      default: "#FFFFFF",
    },
  },
});
