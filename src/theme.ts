import { createTheme } from '@mui/material/styles';

// TODO: Add Dark mode baby
export const theme = createTheme({
  palette: {
    text: {
      primary: '#040316',
    },
    primary: {
      main: '#FFFFFF',
      dark: '#acaaaf',
    },
    secondary: {
      main: '#694FD0',
      dark: '#baaeea',
    },
    background: {
      default: '#FFFFFF',
    },
  },
});
