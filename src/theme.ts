import {createTheme} from "@mui/material/styles";

// TODO: Add Dark mode baby
export const theme = createTheme({
    palette: {
        text: {
            primary: "#040316",
        },
        primary: {
            main: "#FFFFFF",
        },
        secondary: {
            main: "rgb(85, 69, 146)",
        },
        background: {
            default: "#FFFFFF",
        }
    }
});